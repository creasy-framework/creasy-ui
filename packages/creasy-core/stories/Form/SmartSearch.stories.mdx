import { Meta } from '@storybook/addon-docs';
import { ArgsTable } from '@storybook/addon-docs';
import { SmartSearch } from '../../src';

<Meta title="Components/Form/Smart Search" />

## Simple keyword search
```tsx live expanded
render(() => {
  return (
    <div>
        <SmartSearch/>
    </div>
  );
})
```

## With filters

```tsx live
render(() => {
  const filters = [
      { label: 'Color', filterBy: 'color', type: 'SINGLE_SELECT', options: [
          { value: 'r', label: 'Red' },
          { value: 'g', label: 'Green' },
          { value: 'b', label: 'Blue' },
      ]},
      { label: 'Gender', filterBy: 'gender', type: 'SINGLE_SELECT', options: [
          { value: 'm', label: 'Male' },
          { value: 'f', label: 'Female' },
      ]},
      { label: 'Role', filterBy: 'team', type: 'SINGLE_SELECT', options: [
          { value: 'admin', label: 'Admin' },
          { value: 'viewer', label: 'Viewer' },
      ]}
  ];
  return (
    <div>
        <SmartSearch filters={filters}/>
    </div>
  );
})
```

<ArgsTable of={SmartSearch} />
