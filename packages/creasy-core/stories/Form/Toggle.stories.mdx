import { Meta } from '@storybook/addon-docs';
import { ArgsTable } from '@storybook/addon-docs';
import { Toggle } from '../../src';

<Meta title="Components/Form/Toggle" />

## Toggle

```tsx live expanded
render(() => {
  const [checked, setChecked] = React.useState(false);
  return <Toggle label="Toggle" checked={checked} onChange={(e: any) => setChecked(e.target.checked)} />;
})
```

## Disabled

```tsx live
<Toggle label="Disabled" checked={true} disabled />
```

## Read only

```tsx live
<Toggle label="Read only" checked={true} readOnly />
```

## Required

```tsx live
<Toggle label="Required" checked={true} isRequired />
```

<ArgsTable of={Toggle} />
