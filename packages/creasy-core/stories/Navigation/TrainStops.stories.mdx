import { Meta } from '@storybook/addon-docs';
import { ArgsTable } from '@storybook/addon-docs';
import { TrainStops, StopNode } from '../../src';

<Meta title="Components/Navigation/Train Stops" />

## Train stops

```tsx live expanded
render(() => {
    const [activeStep, setActiveStep] = React.useState(1);
    return (
       <TrainStops>
        <StopNode onClick={() => setActiveStep(1)} passed={activeStep > 1} active={activeStep === 1}>Read Aggreement</StopNode>
        <StopNode onClick={() => setActiveStep(2)} passed={activeStep > 2} active={activeStep === 2}>Create Account</StopNode>
        <StopNode onClick={() => setActiveStep(3)} passed={activeStep > 3} active={activeStep === 3}>Choose Course</StopNode>
        <StopNode onClick={() => setActiveStep(4)} passed={activeStep > 4} active={activeStep === 4}>Submit Payment</StopNode>
        <StopNode onClick={() => setActiveStep(5)} passed={activeStep === 5}>Finish</StopNode>
      </TrainStops>
    )
})
```

#### TrainStops
<ArgsTable of={TrainStops} />

#### StopNode
<ArgsTable of={StopNode} />
